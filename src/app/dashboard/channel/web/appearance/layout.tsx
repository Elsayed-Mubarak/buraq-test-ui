import React from 'react'


export const metadata = {
    title: 'Appearance | Web Configuration | Buraq',
}

type Props = {
    children: React.ReactNode
}

export default function AppearanceLayout({ children }: Props) {
    return children
}