export const metadata = {
    title: 'Popup Messages | Web Configuration | Buraq',
}
type Props = {
    children: React.ReactNode
}

export default function PopupMessagesLayout({ children }: Props) {
    return children
}