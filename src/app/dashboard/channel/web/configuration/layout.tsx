import React from 'react'
export const metadata = {
    title: 'Web Configuration | Buraq',
}
type Props = {
    children: React.ReactNode
}

export default function layout({ children }: Props) {
    return children
}