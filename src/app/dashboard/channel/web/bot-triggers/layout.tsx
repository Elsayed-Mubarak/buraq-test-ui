import React from 'react'

type Props = {
    children: React.ReactNode
}
export const metadata = {
    title: 'Trigger Conditions | Web Configuration | Buraq',
}
export default function BotTriggersLayout({ children }: Props) {
    return children
}