
export const metadata = {
    title: 'General Settings | Web Configuration | Buraq',
}
type Props = {
    children: React.ReactNode
}

export default function GeneralSettingsLayout({ children }: Props) {
    return children
}